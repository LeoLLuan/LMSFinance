@model IEnumerable<LMSFinance.Models.TeacherFaculty>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Faculty</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TeaFaculty)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id=item.TeaFaculty })
        </td>
    </tr>
}

</table>
