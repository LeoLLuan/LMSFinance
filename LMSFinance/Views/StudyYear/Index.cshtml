@model IEnumerable<LMSFinance.Models.StudyYear>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            School Year
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SchoolYear)
        </td>
        <td>
            @Html.ActionLink("Delete", "Delete", new { id = item.SchoolYear })
        </td>
    </tr>
}

</table>
