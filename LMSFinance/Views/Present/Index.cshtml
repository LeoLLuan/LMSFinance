@model IEnumerable<LMSFinance.Models.Present>

@{
    ViewBag.Title = "Index";
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Prize Name
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelitem => item.Presents)
            </td>
            <td>
                @Html.ActionLink("Delete", "Delete", new { id = item.Presents })
            </td>
        </tr>
    }
</table>
